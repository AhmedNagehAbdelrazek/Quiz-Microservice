const DifficultyTypes = require("./difficulty-types");
const GradingTypes = require("./grading-types");
const QuestionsTypes = require("./question-types");

module.exports = { DifficultyTypes, GradingTypes, QuestionsTypes };
